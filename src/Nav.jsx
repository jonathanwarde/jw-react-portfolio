const Nav = (props) => {
    return (
        <nav>
            <button>home</button>
            <button>projects</button>
            <button>services</button>
            <button>blog</button>
            <button>contact</button>
        </nav>
    );
};

export default Nav;
